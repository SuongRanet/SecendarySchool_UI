import { PARENT_NAVIGATION } from '@/app/navigation';
import { DashboardShell } from './DashboardShell';

/** Layout for parents and guardians monitoring their children. */
export const ParentLayout = () => <DashboardShell navigation={PARENT_NAVIGATION} />;
