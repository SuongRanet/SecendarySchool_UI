import { TEACHER_NAVIGATION } from '@/app/navigation';
import { DashboardShell } from './DashboardShell';

/** Layout for teachers and homeroom teachers. */
export const TeacherLayout = () => <DashboardShell navigation={TEACHER_NAVIGATION} />;
