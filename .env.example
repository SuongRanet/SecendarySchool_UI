# Base URL of the backend API, including the version prefix.
VITE_API_URL=http://localhost:3000/api/v1

# Name shown in the sidebar, the browser title and on printed report cards.
VITE_APP_NAME=Hun Sen Turey

# Default interface language: en | kh
VITE_DEFAULT_LANGUAGE=en
